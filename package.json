{
  "scripts": {
    "build": "cd app-principal && npm install && npm run build && cd .. && rm -rf public && mv app-principal/build public && mkdir -p public/sabores && cp -r sabores-page/. public/sabores/"
  }
}